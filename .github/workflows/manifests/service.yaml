apiVersion: v1
kind: Service
metadata:
  name: node-rate-limit-svc
  namespace: test
spec:
  selector:
    app: node-rate-limit
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
